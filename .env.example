# Exemplo de variáveis de ambiente para o projeto TechStore
# Renomeie este arquivo para .env.local e preencha com seus valores

# Chave secreta para JWT (JSON Web Token)
# Gere uma string aleatória e forte, por exemplo: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here

# URL da API (se você tiver uma API externa, caso contrário, use a URL base do seu app Next.js)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
